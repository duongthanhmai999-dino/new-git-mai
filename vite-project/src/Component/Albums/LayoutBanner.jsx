const LayoutBanner = ({ layoutImg }) => {
    return (
        <div className="albums-layout-wrap">
            <img src={layoutImg} alt="layout3" className="albums-layout3" />
        </div>
    );
};

export default LayoutBanner;
